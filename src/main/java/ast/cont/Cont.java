package ast.cont;

import ast.ASTNode;

/**
 * This abstract class represents a continuation in the abstract syntax tree (AST) of queries.
 * It extends the QueryI class and implements the ASTNode class.
 */
public interface Cont
    extends ASTNode<Void> {
}
